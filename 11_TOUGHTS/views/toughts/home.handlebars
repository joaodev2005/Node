{{!-- <div class="home-header">
  {{#if search}}
    <h1>Resultados para: <span> {{search}}</span></h1>
  {{else}}
    <h1>Conheça alguns do nossos <span>Pensamentos</span>:</h1>
    {{#if toughtsQty}}
      <p>Quantidade de pensamentos: {{toughtsQty}}</p>
    {{else}}
      <h1>Não há pensamentos cadastrados!</h1>
    {{/if}}
  {{/if}}
  <form action="/" method="get">
    <input type="text" name="search" placeholder="Pesquisar pensamentos" />
    <input type="submit" class="btn" value="Pesquisar" />
  </form>
  <div class="order-container">
    <span>Ordenar por: </span>
    <form action="/" method="get">
      <input type="hidden" name="search" value="{{this.search}}" />
      <input type="hidden" name="order" value="new" />
      <button type="submit"><i class="bi bi-arrow-up"></i></button>
    </form>
    <form action="/" method="get">
      <input type="hidden" name="search" value="{{this.search}}" />
      <input type="hidden" name="order" value="old" />
      <button type="submit"><i class="bi bi-arrow-down"></i></button>
    </form>
    <a href="/">Limpar</a>
  </div>
</div>
<div class="toughts-container">
  {{#each toughts}}
    <figure>
      <blockquote>
        <p>{{this.title}}</p>
      </blockquote>
      <figcaption>por <span>{{this.User.name}}</span></figcaption>
    </figure>
  {{/each}}
</div> --}}

<div class="home-header">
    {{#if search}}
    <h1>Você está buscando por: <span>{{search}}</span></h1>
        {{#if toughtsQty }}
            <p>Foram encontrados <span>{{toughtsQty}}</span> pensamento(s)</p>
        {{else}}
            <p>Nenhum pensamento encontrado para o termo <span>{{search}}</span></p>
        {{/if}}
    {{else}}
    <h1>Conheça alguns dos nossos <span>Pensamentos</span>:</h1>
    {{/if}}
    <form action="/" method="GET">
        <input type="text" name="search" placeholder="Está buscando por algo?">
        <input type="submit" class="btn" value="Buscar">
    </form>
    <div class="order-container">
        <span>Ordenar por:</span>  
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="new">
            <button type="submit"><i class="bi bi-arrow-up"></i> Mais novos</button>
        </form>
        <form action="/" method="GET">
            <input type="hidden" name="search" value="{{this.search}}">
            <input type="hidden" name="order" value="old">
            <button type="submit"><i class="bi bi-arrow-down"></i> Mais antigos</button>
        </form>
        <a href="/">Limpar</a>
    </div>
</div>
<div class="toughts-container">
    {{#each toughts}}
        <figure>
            <blockquote>
                "{{this.title}}"
            </blockquote>
            <figcaption>por <span>{{this.User.name}}</span></figcaption>
        </figure>
    {{/each}}
</div>