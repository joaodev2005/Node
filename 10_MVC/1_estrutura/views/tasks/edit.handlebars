<h1>Editando a tarefa: {{task.title}}</h1>
<form action="/tasks/add" method="POST" class="form-control">
    <input type="hidden" name="id" value="{{task.id}}">
    <div class="form-control">
        <label for="title">Titulo:</label>
        <input type="text" name="title" placeholder="Digite o titulo" value="{{task.title}}" required />
    </div>
     <div class="form-control">
        <label for="description">Descrição:</label>
        <textarea name="description" placeholder="Digite a descrição" required>{{task.description}}</textarea>
    </div>
    <input type="submit" value="Editar" />
</form>